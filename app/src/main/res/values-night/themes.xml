<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HangedMan" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="backgroundColor">@color/background_color_1_dark</item>
        <item name="background">@color/background_color_2_dark</item>
        <item name="headerLayout">@color/header_background_dark</item>
        <item name="textFillColor">@color/text_color_dark</item>

        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
    </style>

    <style name="Theme.HangedMan" parent="Base.Theme.HangedMan" />
</resources>